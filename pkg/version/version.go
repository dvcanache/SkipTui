package version

var (
	// Version is the current semantic version of SkipTUI.
	Version = "0.1.0"
	// GitCommit is the git commit hash set at build time.
	GitCommit = "dev"
	// BuildDate is the build date set at build time.
	BuildDate = "unknown"
)
